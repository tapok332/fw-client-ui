# Audit Fix Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fix all P0-P2 issues from the impeccable technical audit (1 P0, 24 P1, 28 P2) to raise the audit score from 8/20 to 14+/20.

**Architecture:** Seven sequential task waves organized by impeccable skill: harden (accessibility), normalize (theming), optimize (performance), adapt (responsive), distill (simplify), animate (motion), polish (final pass). Each wave produces a working build and a commit.

**Tech Stack:** Next.js 15, React 18, TypeScript, Tailwind CSS, Framer Motion, Radix UI, Lucide Icons.

---

## File Map

### Wave 1 — Harden (Accessibility)
- Modify: `src/app/orders/history/page.tsx:159-161` — div→Link
- Modify: `src/components/Header.tsx:136,170,111` — aria-labels, focus ring
- Modify: `src/components/LanguageSwitcher.tsx:19` — aria-label
- Modify: `src/components/header/profile-dropdown.tsx:40` — aria-label
- Modify: `src/app/login/page.tsx:229-241` — aria-label
- Modify: `src/app/register/page.tsx:155,200,208-220` — aria-label + aria-describedby
- Modify: `src/components/ui/avatar-upload.tsx:91-121` — role, tabIndex, aria-label
- Modify: `src/app/not-found.tsx:6` — h2→h1
- Modify: `src/app/error.tsx:39` — lang attribute
- Modify: 5 back-link pages: `src/app/support/page.tsx`, `src/app/activate-code/page.tsx`, `src/app/payment-methods/page.tsx`, `src/app/addresses/page.tsx`, `src/app/profile/edit/page.tsx` — aria-labels on ArrowLeft links

### Wave 2 — Normalize (Theming)
- Modify: `src/components/ui/skeleton.tsx:9` — bg-gray-200→bg-muted
- Modify: `src/app/activate-code/page.tsx:87` — hex→primary
- Modify: `src/app/profile/edit/page.tsx:226` — hex→primary
- Modify: `src/app/restaurants/page.tsx:490,535,566,603` — hex→primary
- Modify: `src/app/checkout/confirmation/page.tsx:305,349,389,434,455` — wise-green/green-500→primary
- Modify: `src/components/cart/cart-indicator.tsx:63,75` — green-500→primary
- Modify: `src/components/store/add-to-cart-button.tsx:105` — wise-green→primary
- Modify: `src/components/stores/store-view.tsx` — bg-white→bg-card, text-gray→semantic tokens (25+ occurrences)
- Modify: `src/app/checkout/page.tsx` — text-gray→semantic, border-gray→border
- Modify: `src/components/header/profile-dropdown.tsx:43` — text-white→text-primary-foreground
- Modify: `src/components/animation/fly-to-cart.tsx:175,274` — hex→CSS variable
- Modify: `src/app/orders/[id]/page.tsx:155-163` — status badges add dark variants
- Modify: `src/components/Header.tsx:67-68,80,107-109,126,140,175,180` — gray→semantic tokens

### Wave 3 — Optimize (Performance)
- Modify: `src/components/Header.tsx:91-95` — width animation→scaleX
- Modify: `src/components/store/add-to-cart-button.tsx:33,37,63,79,95` — remove console.log
- Modify: `src/components/animation/fly-to-cart.tsx` — add will-change
- Modify: `src/components/home/hero-carousel.tsx` — add will-change
- Modify: `src/components/home/surprise-box-card.tsx` — add will-change
- Modify: `src/components/cart/cart-indicator.tsx` — add will-change

### Wave 4 — Adapt (Responsive)
- Modify: `src/components/profile/stats-section.tsx:21` — grid-cols-4→responsive
- Modify: `src/app/profile/page.tsx:243` — grid-cols-3→responsive
- Modify: `src/components/MobileNavigation.tsx:73` — text-[10px]→text-[11px]
- Modify: `src/components/home/hero-carousel.tsx:154-165` — enlarge dot touch targets
- Modify: `src/app/cart/page.tsx:200` — enlarge delete button
- Modify: `src/app/checkout/page.tsx:485` — enlarge edit button

### Wave 5 — Distill (Simplify)
- Modify: `src/app/profile/page.tsx` — remove duplicate stats-section if present, keep eco cards only
- Modify: `src/app/checkout/confirmation/page.tsx` — flatten card-in-card nesting

### Wave 6 — Animate (Motion)
- Modify: `src/components/store/store-card.tsx:24` — spring→tween ease-out
- Modify: `src/components/home/surprise-box-card.tsx:89` — spring→tween ease-out
- Modify: `src/app/checkout/confirmation/page.tsx:244` — spring→tween ease-out
- Keep spring ONLY on: Header cart badge (functional feedback), MobileNavigation tap (tactile)

### Wave 7 — Polish (Final)
- Modify: `src/app/activate-code/page.tsx:97` — h3→h2
- Modify: `src/app/support/page.tsx:116,123,130` — add h2 before h3s
- Modify: `src/app/addresses/page.tsx:271` — h3→h2
- Modify: `src/app/payment-methods/page.tsx:135` — h3→h2
- Modify: `src/components/home/hero-carousel.tsx:122` — fix alt text ternary
- Modify: `src/components/stores/store-view.tsx:253,344,554,709` — fix placeholder alt text

---

## Wave 1: Harden (Accessibility) — `/harden`

### Task 1.1: Fix P0 — Order history keyboard accessibility

**Files:**
- Modify: `src/app/orders/history/page.tsx:159-161`

- [ ] **Step 1: Replace div with Link**

Change lines 159-161 from:
```tsx
<div
    className="p-4 cursor-pointer"
    onClick={() => router.push(`/orders/${order.id}`)}
>
```
to:
```tsx
<Link
    href={`/orders/${order.id}`}
    className="block p-4 cursor-pointer hover:bg-muted/50 transition-colors rounded-xl"
>
```

Add `import Link from "next/link";` at top if not already imported. Change the closing `</div>` to `</Link>`.

- [ ] **Step 2: Verify build**

Run: `npx tsc --noEmit 2>&1 | head -10`
Expected: No errors

- [ ] **Step 3: Commit**

```bash
git add src/app/orders/history/page.tsx
git commit -m "fix(a11y): make order history cards keyboard-accessible with Link"
```

### Task 1.2: Add aria-labels to all icon-only interactive elements

**Files:**
- Modify: `src/components/Header.tsx:136,170,111`
- Modify: `src/components/LanguageSwitcher.tsx:19`
- Modify: `src/components/header/profile-dropdown.tsx:40`
- Modify: `src/app/login/page.tsx:229`
- Modify: `src/app/register/page.tsx:208`

- [ ] **Step 1: Header — cart and profile links**

In `src/components/Header.tsx`:

Line 136 — add `aria-label` to cart Link:
```tsx
<Link
    href="/cart"
    aria-label={t("navigation", "cart")}
    className="..."
>
```

Line 170 — add `aria-label` to profile Link:
```tsx
<Link
    href="/profile"
    onClick={handleProfileClick}
    aria-label={t("navigation", "profile")}
    className="..."
>
```

Line 111 — strengthen focus ring from `focus:ring-1 focus:ring-primary/40` to:
```
focus:outline-none focus:ring-2 focus:ring-primary/60
```

- [ ] **Step 2: LanguageSwitcher**

In `src/components/LanguageSwitcher.tsx` line 19, add `aria-label`:
```tsx
<Button variant="ghost" size="icon" className="w-9 px-0" aria-label="Change language">
```

- [ ] **Step 3: Profile dropdown trigger**

In `src/components/header/profile-dropdown.tsx` line 40, add `aria-label`:
```tsx
<button className="relative flex h-8 w-8 items-center justify-center rounded-full" aria-label="Profile menu">
```

- [ ] **Step 4: Password toggle buttons**

In `src/app/login/page.tsx` line 229, add:
```tsx
<Button
    type="button"
    variant="ghost"
    size="sm"
    aria-label={showPassword ? "Hide password" : "Show password"}
    className="..."
    onClick={() => setShowPassword(!showPassword)}
>
```

Same change in `src/app/register/page.tsx` line 208.

- [ ] **Step 5: Commit**

```bash
git add src/components/Header.tsx src/components/LanguageSwitcher.tsx src/components/header/profile-dropdown.tsx src/app/login/page.tsx src/app/register/page.tsx
git commit -m "fix(a11y): add aria-labels to all icon-only interactive elements"
```

### Task 1.3: Fix back-link aria-labels across 5 pages

**Files:**
- Modify: `src/app/support/page.tsx`, `src/app/activate-code/page.tsx`, `src/app/payment-methods/page.tsx`, `src/app/addresses/page.tsx`, `src/app/profile/edit/page.tsx`

- [ ] **Step 1: Add aria-label to each ArrowLeft Link**

In each file, find the pattern:
```tsx
<Link href="/profile" className="mr-2">
    <ArrowLeft className="w-5 h-5" />
</Link>
```

Add `aria-label={t("common", "back")}` (or hardcode `aria-label="Back"` if `t` is not available in that file):
```tsx
<Link href="/profile" className="mr-2" aria-label="Back">
    <ArrowLeft className="w-5 h-5" />
</Link>
```

Do this in all 5 files.

- [ ] **Step 2: Commit**

```bash
git add src/app/support/page.tsx src/app/activate-code/page.tsx src/app/payment-methods/page.tsx src/app/addresses/page.tsx src/app/profile/edit/page.tsx
git commit -m "fix(a11y): add aria-labels to back navigation links"
```

### Task 1.4: Fix avatar upload accessibility

**Files:**
- Modify: `src/components/ui/avatar-upload.tsx:91-121`

- [ ] **Step 1: Make Avatar keyboard-accessible**

At line 91, the `<Avatar>` with `onClick` needs `role`, `tabIndex`, and keyboard handler. Change:
```tsx
<Avatar
    className="... cursor-pointer"
    onClick={handleClick}
>
```
to:
```tsx
<Avatar
    className="... cursor-pointer"
    onClick={handleClick}
    onKeyDown={(e: React.KeyboardEvent) => { if (e.key === 'Enter' || e.key === ' ') { e.preventDefault(); handleClick(); } }}
    role="button"
    tabIndex={0}
    aria-label="Upload profile photo"
>
```

At line 114, add aria-label to hidden input:
```tsx
<input
    ref={fileInputRef}
    type="file"
    accept="image/*"
    className="hidden"
    aria-label="Upload profile photo"
    onChange={...}
/>
```

- [ ] **Step 2: Commit**

```bash
git add src/components/ui/avatar-upload.tsx
git commit -m "fix(a11y): make avatar upload keyboard-accessible with role and aria-label"
```

### Task 1.5: Fix register password aria-describedby

**Files:**
- Modify: `src/app/register/page.tsx:155,200`

- [ ] **Step 1: Link requirements to input**

At line 155, add `id` to the requirements list:
```tsx
<ul id="password-requirements" className="space-y-1 mt-2">
```

At line 200, add `aria-describedby` to the password Input:
```tsx
<Input
    id="password"
    aria-describedby="password-requirements"
    type={showPassword ? "text" : "password"}
    ...
/>
```

- [ ] **Step 2: Commit**

```bash
git add src/app/register/page.tsx
git commit -m "fix(a11y): link password requirements to input via aria-describedby"
```

### Task 1.6: Fix semantic HTML issues

**Files:**
- Modify: `src/app/not-found.tsx:6`
- Modify: `src/app/error.tsx:39`

- [ ] **Step 1: not-found.tsx — change h2 to h1**

Line 6: Change `<h2` to `<h1` and closing `</h2>` to `</h1>`.

- [ ] **Step 2: error.tsx — add lang attribute**

Line 39: Change `<html>` to `<html lang="uk">`.

- [ ] **Step 3: Commit**

```bash
git add src/app/not-found.tsx src/app/error.tsx
git commit -m "fix(a11y): correct heading hierarchy and add lang attribute to error page"
```

---

## Wave 2: Normalize (Theming) — `/normalize`

### Task 2.1: Fix skeleton component

**Files:**
- Modify: `src/components/ui/skeleton.tsx:9`

- [ ] **Step 1: Replace bg-gray-200 with bg-muted**

Line 9: Change `bg-gray-200` to `bg-muted`:
```tsx
className={cn("animate-pulse rounded-md bg-muted", className)}
```

- [ ] **Step 2: Commit**

```bash
git add src/components/ui/skeleton.tsx
git commit -m "fix(theme): use bg-muted token in skeleton component"
```

### Task 2.2: Replace hard-coded hex colors with primary token

**Files:**
- Modify: `src/app/activate-code/page.tsx:87`
- Modify: `src/app/profile/edit/page.tsx:226`
- Modify: `src/app/restaurants/page.tsx:490,535,566,603`

- [ ] **Step 1: activate-code and profile/edit**

In `src/app/activate-code/page.tsx` line 87, replace:
```
bg-[#34a853] hover:bg-[#2d9549] text-white
```
with:
```
bg-primary hover:bg-primary/90 text-primary-foreground
```

Same change in `src/app/profile/edit/page.tsx` line 226.

- [ ] **Step 2: restaurants page**

In `src/app/restaurants/page.tsx`, replace all 4 occurrences of `text-[#38A240]` with `text-primary`.

- [ ] **Step 3: Commit**

```bash
git add src/app/activate-code/page.tsx src/app/profile/edit/page.tsx src/app/restaurants/page.tsx
git commit -m "fix(theme): replace hard-coded hex colors with primary token"
```

### Task 2.3: Consolidate wise-green/green-500 to primary in confirmation page

**Files:**
- Modify: `src/app/checkout/confirmation/page.tsx`

- [ ] **Step 1: Replace all token collisions**

Replace all occurrences of `bg-wise-green` with `bg-primary`.
Replace all occurrences of `text-green-500` with `text-primary`.
Replace all occurrences of `text-green-600` with `text-primary`.
Replace all occurrences of `bg-green-500` with `bg-primary`.
Replace all occurrences of `bg-green-100` with `bg-primary/10`.
Replace all occurrences of `text-green-700` with `text-primary`.
Replace all occurrences of `bg-green-50` with `bg-primary/5`.
Replace all occurrences of `hover:bg-green-50` with `hover:bg-primary/5`.
Replace all occurrences of `hover:bg-green-600` with `hover:bg-primary/90`.

Fix the identical ternary at line 455 — both branches return `"text-green-500"`. Replace entire ternary with just `"text-sm text-primary"`.

- [ ] **Step 2: Commit**

```bash
git add src/app/checkout/confirmation/page.tsx
git commit -m "fix(theme): consolidate green tokens to primary in confirmation page"
```

### Task 2.4: Consolidate tokens in cart-indicator, add-to-cart-button, profile-dropdown

**Files:**
- Modify: `src/components/cart/cart-indicator.tsx:63,75`
- Modify: `src/components/store/add-to-cart-button.tsx:105`
- Modify: `src/components/header/profile-dropdown.tsx:43`

- [ ] **Step 1: cart-indicator**

Replace `text-green-500` with `text-primary`, `bg-green-500` with `bg-primary`, `hover:bg-green-50` with `hover:bg-primary/5`, `bg-green-50` with `bg-primary/5`.

- [ ] **Step 2: add-to-cart-button**

Replace `bg-wise-green` with `bg-primary` at line 105.

- [ ] **Step 3: profile-dropdown**

Replace `text-white` with `text-primary-foreground` at line 43.

- [ ] **Step 4: Commit**

```bash
git add src/components/cart/cart-indicator.tsx src/components/store/add-to-cart-button.tsx src/components/header/profile-dropdown.tsx
git commit -m "fix(theme): consolidate green tokens in cart indicator, add-to-cart, and profile dropdown"
```

### Task 2.5: Replace bg-white/text-gray with semantic tokens in Header

**Files:**
- Modify: `src/components/Header.tsx`

- [ ] **Step 1: Replace all hard-coded grays**

- Line 67: `bg-white/90 dark:bg-gray-900/90` → `bg-background/90`
- Line 68: `bg-white/80 dark:bg-gray-900/80` → `bg-background/80`
- Line 80: `text-gray-900 dark:text-white` → `text-foreground`
- Line 107: `bg-gray-100/80 dark:bg-gray-800/80` → `bg-muted/80`
- Line 108: `text-gray-700 dark:text-gray-200` → `text-foreground`
- Line 109: `placeholder:text-gray-400 dark:placeholder:text-gray-500` → `placeholder:text-muted-foreground`
- Line 126: `text-gray-500 dark:text-gray-400` → `text-muted-foreground`
- Line 140: `text-gray-500 dark:text-gray-400` → `text-muted-foreground`
- Line 175: `text-gray-500 dark:text-gray-400` → `text-muted-foreground`
- Line 180: `bg-gray-100 dark:bg-gray-800` → `bg-muted`

- [ ] **Step 2: Commit**

```bash
git add src/components/Header.tsx
git commit -m "fix(theme): replace hard-coded gray tokens with semantic tokens in Header"
```

### Task 2.6: Add dark mode to store-view (top priority dark-mode gap)

**Files:**
- Modify: `src/components/stores/store-view.tsx`

- [ ] **Step 1: Bulk replace in store-view**

Replace all `bg-white` (non-overlay contexts) with `bg-card`.
Replace all `text-gray-700` (without dark:) with `text-foreground`.
Replace all `text-gray-500` (without dark:) with `text-muted-foreground`.
Replace all `text-gray-600` (without dark:) with `text-muted-foreground`.
Replace all `bg-gray-50` (without dark:) with `bg-muted/50`.
Replace all `bg-gray-100` (without dark:) with `bg-muted`.
Replace all `border-gray-200` (without dark:) with `border-border`.
Replace all `border-gray-300` (without dark:) with `border-border`.
Replace `text-green-600` / `text-green-500` with `text-primary`.
Replace `bg-green-500` with `bg-primary`.
Replace `hover:text-green-600` with `hover:text-primary`.
Replace `hover:border-green-500` with `hover:border-primary`.

Do NOT touch: Google Maps inline styles, SVG fill attributes, or overlay/gradient contexts.

- [ ] **Step 2: Verify build**

Run: `npx tsc --noEmit 2>&1 | head -10`

- [ ] **Step 3: Commit**

```bash
git add src/components/stores/store-view.tsx
git commit -m "fix(theme): add semantic tokens and dark mode support to store-view"
```

### Task 2.7: Add dark variants to status badges

**Files:**
- Modify: `src/app/orders/[id]/page.tsx:155-163`

- [ ] **Step 1: Add dark variants to each status color**

Replace:
```tsx
{ color: 'bg-yellow-100 text-yellow-800' }
```
with:
```tsx
{ color: 'bg-yellow-100 dark:bg-yellow-900/30 text-yellow-800 dark:text-yellow-300' }
```

Apply same pattern for blue, green, and red status colors.

- [ ] **Step 2: Commit**

```bash
git add src/app/orders/[id]/page.tsx
git commit -m "fix(theme): add dark mode variants to order status badges"
```

---

## Wave 3: Optimize (Performance) — `/optimize`

### Task 3.1: Fix search bar width animation

**Files:**
- Modify: `src/components/Header.tsx:91-95`

- [ ] **Step 1: Replace width animation with scaleX**

Change:
```tsx
<motion.form
    initial={{ width: 0, opacity: 0 }}
    animate={{ width: 220, opacity: 1 }}
    exit={{ width: 0, opacity: 0 }}
    transition={{ duration: 0.3, ease: "easeInOut" }}
```
to:
```tsx
<motion.form
    initial={{ scaleX: 0, opacity: 0 }}
    animate={{ scaleX: 1, opacity: 1 }}
    exit={{ scaleX: 0, opacity: 0 }}
    transition={{ duration: 0.3, ease: "easeInOut" }}
    style={{ transformOrigin: "right", width: 220 }}
```

- [ ] **Step 2: Commit**

```bash
git add src/components/Header.tsx
git commit -m "perf: replace width animation with scaleX transform in search bar"
```

### Task 3.2: Remove console.log from add-to-cart-button

**Files:**
- Modify: `src/components/store/add-to-cart-button.tsx:33,37,63,79,95`

- [ ] **Step 1: Remove all console.log lines**

Delete lines 33, 37, 63, 79, 95 (the `console.log` calls). Keep the `console.error` at line 89 — error logging is acceptable.

- [ ] **Step 2: Commit**

```bash
git add src/components/store/add-to-cart-button.tsx
git commit -m "perf: remove console.log from add-to-cart hot path"
```

### Task 3.3: Add will-change to animated components

**Files:**
- Modify: `src/components/animation/fly-to-cart.tsx`
- Modify: `src/components/home/hero-carousel.tsx`
- Modify: `src/components/home/surprise-box-card.tsx`
- Modify: `src/components/cart/cart-indicator.tsx`

- [ ] **Step 1: Add will-change to fly-to-cart motion.div**

In `fly-to-cart.tsx`, on the main animated `motion.div`, add `style={{ willChange: "transform, opacity" }}`.

- [ ] **Step 2: Add will-change to hero-carousel slide**

In `hero-carousel.tsx`, on the `motion.div` at line 112, add `style={{ willChange: "transform, opacity" }}`.

- [ ] **Step 3: Add will-change to surprise-box-card**

In `surprise-box-card.tsx`, on the `motion.div` at line 85, add to existing className or style: `style={{ willChange: "transform" }}`.

- [ ] **Step 4: Add will-change to cart-indicator**

In `cart-indicator.tsx`, on the animated badge `motion.div`, add `style={{ willChange: "transform" }}`.

- [ ] **Step 5: Commit**

```bash
git add src/components/animation/fly-to-cart.tsx src/components/home/hero-carousel.tsx src/components/home/surprise-box-card.tsx src/components/cart/cart-indicator.tsx
git commit -m "perf: add will-change hints to animated components"
```

---

## Wave 4: Adapt (Responsive) — `/adapt`

### Task 4.1: Fix stats grids responsive breakpoints

**Files:**
- Modify: `src/components/profile/stats-section.tsx:21`
- Modify: `src/app/profile/page.tsx:243`

- [ ] **Step 1: stats-section.tsx**

Line 21: Change `grid grid-cols-4` to `grid grid-cols-2 sm:grid-cols-4`:
```tsx
<CardContent className="grid grid-cols-2 sm:grid-cols-4 gap-4 text-center">
```

- [ ] **Step 2: profile/page.tsx eco cards**

Line 243: Change `grid grid-cols-3` to `grid grid-cols-1 sm:grid-cols-3`:
```tsx
<motion.div {...fadeUp(0.1)} className="grid grid-cols-1 sm:grid-cols-3 gap-3">
```

- [ ] **Step 3: Commit**

```bash
git add src/components/profile/stats-section.tsx src/app/profile/page.tsx
git commit -m "fix(responsive): add mobile breakpoints to stats grids"
```

### Task 4.2: Enlarge touch targets

**Files:**
- Modify: `src/components/MobileNavigation.tsx:73`
- Modify: `src/components/home/hero-carousel.tsx:154-165`
- Modify: `src/app/cart/page.tsx:200`
- Modify: `src/app/checkout/page.tsx:485`

- [ ] **Step 1: Mobile nav labels**

Line 73: Change `text-[10px]` to `text-[11px]`:
```tsx
"text-[11px] mt-0.5 transition-colors duration-200",
```

- [ ] **Step 2: Carousel dots — add padding for touch area**

In `hero-carousel.tsx`, wrap each dot button in a larger touch area. Change the button className to include minimum padding:
```tsx
className={`h-2.5 rounded-full transition-all duration-300 cursor-pointer p-2 -m-2 ${
    idx === currentSlide
        ? "w-6 bg-primary"
        : "w-2.5 bg-white/50 hover:bg-white/70"
}`}
```

The `p-2 -m-2` creates a 44px touch target around the 10px visual dot using negative margin to prevent layout shift.

- [ ] **Step 3: Cart delete button**

In `cart/page.tsx` line 200, change `p-2` to `p-2.5`:
```tsx
className="p-2.5 rounded-full bg-muted/50 hover:bg-destructive/10 transition-colors cursor-pointer"
```

- [ ] **Step 4: Checkout edit button**

In `checkout/page.tsx` line 485, change `p-1` to `p-2`:
```tsx
className="ml-auto p-2 rounded-lg hover:bg-muted transition-colors cursor-pointer"
```

- [ ] **Step 5: Commit**

```bash
git add src/components/MobileNavigation.tsx src/components/home/hero-carousel.tsx src/app/cart/page.tsx src/app/checkout/page.tsx
git commit -m "fix(responsive): enlarge touch targets to meet 44px minimum"
```

---

## Wave 5: Distill (Simplify) — `/distill`

### Task 5.1: Remove duplicate hero-metric stats from profile

**Files:**
- Modify: `src/app/profile/page.tsx`

- [ ] **Step 1: Check if stats-section.tsx is rendered on profile page**

Search for `StatsSection` or `stats-section` import in `src/app/profile/page.tsx`. If the `stats-section.tsx` component is imported AND the eco-impact cards are also rendered, remove the `stats-section` import and usage — keep only the eco-impact cards (which are more visually refined).

If `stats-section.tsx` is NOT imported in profile page, no change needed here.

- [ ] **Step 2: Commit (if changed)**

```bash
git add src/app/profile/page.tsx
git commit -m "fix(design): remove duplicate hero-metric stats grid from profile"
```

### Task 5.2: Flatten card-in-card in confirmation page

**Files:**
- Modify: `src/app/checkout/confirmation/page.tsx`

- [ ] **Step 1: Replace inner bg-gray-50 boxes with flat sections**

Find inner divs styled as `bg-gray-50 p-3 rounded-lg` inside Card components (approximately lines 516 and 571). Replace `bg-gray-50` with `bg-muted/30` and remove any extra shadow or border that creates the "inner card" effect.

- [ ] **Step 2: Commit**

```bash
git add src/app/checkout/confirmation/page.tsx
git commit -m "fix(design): flatten card-in-card nesting in confirmation page"
```

---

## Wave 6: Animate (Motion) — `/animate`

### Task 6.1: Replace spring bounce with smooth easing

**Files:**
- Modify: `src/components/store/store-card.tsx:24`
- Modify: `src/components/home/surprise-box-card.tsx:89`
- Modify: `src/app/checkout/confirmation/page.tsx:244`

- [ ] **Step 1: store-card.tsx**

Line 24: Change:
```tsx
transition={{ type: "spring", stiffness: 300, damping: 20 }}
```
to:
```tsx
transition={{ duration: 0.25, ease: [0.25, 0.46, 0.45, 0.94] }}
```

(This is ease-out-quad — smooth deceleration without bounce.)

- [ ] **Step 2: surprise-box-card.tsx**

Line 89: Same change — replace `type: "spring"` with:
```tsx
transition={{ duration: 0.25, ease: [0.25, 0.46, 0.45, 0.94] }}
```

- [ ] **Step 3: confirmation checkmark**

Line 244 (the success checkmark scale-in): Change:
```tsx
transition={{ type: "spring", stiffness: 300, damping: 15, delay: 0.3 }}
```
to:
```tsx
transition={{ duration: 0.4, ease: [0.16, 1, 0.3, 1], delay: 0.3 }}
```

(This is ease-out-expo — fast decel, no bounce. Better for a celebratory moment.)

- [ ] **Step 4: Do NOT change** Header cart badge spring or MobileNavigation tap spring — these are functional feedback, not decorative.

- [ ] **Step 5: Commit**

```bash
git add src/components/store/store-card.tsx src/components/home/surprise-box-card.tsx src/app/checkout/confirmation/page.tsx
git commit -m "fix(motion): replace spring bounce with smooth exponential easing"
```

---

## Wave 7: Polish (Final) — `/polish`

### Task 7.1: Fix heading hierarchy across pages

**Files:**
- Modify: `src/app/activate-code/page.tsx:97`
- Modify: `src/app/support/page.tsx:116,123,130`
- Modify: `src/app/addresses/page.tsx:271`
- Modify: `src/app/payment-methods/page.tsx:135`

- [ ] **Step 1: Fix h3→h2 in activate-code**

Line 97: Change `<h3` to `<h2` and `</h3>` to `</h2>`.

- [ ] **Step 2: Fix heading hierarchy in support**

Before line 116 (the first h3), add an h2 section heading:
```tsx
<h2 className="text-lg font-semibold font-[family-name:var(--font-heading)] mb-3">FAQ</h2>
```

Then change the `h3` elements at lines 116, 123, 130 to keep them as `h3` (they're now properly nested under h2).

- [ ] **Step 3: Fix h3→h2 in addresses and payment-methods**

In `addresses/page.tsx` line 271 and `payment-methods/page.tsx` line 135: Change `<h3` to `<h2` and closing tags.

- [ ] **Step 4: Commit**

```bash
git add src/app/activate-code/page.tsx src/app/support/page.tsx src/app/addresses/page.tsx src/app/payment-methods/page.tsx
git commit -m "fix(a11y): correct heading hierarchy across pages"
```

### Task 7.2: Fix alt text issues

**Files:**
- Modify: `src/components/home/hero-carousel.tsx:122`
- Modify: `src/components/stores/store-view.tsx:253,344,554,709`

- [ ] **Step 1: Fix carousel alt text**

Line 122: The ternary returns the same value in both branches. Replace:
```tsx
alt={carouselImages[currentSlide]?.key ? t("home", "promotionalCarousel") : t("home", "promotionalCarousel")}
```
with:
```tsx
alt={`${t("home", "promotionalCarousel")} - ${currentSlide + 1}`}
```

- [ ] **Step 2: Fix placeholder alt text in store-view**

Replace all `alt="Store Cover Placeholder"`, `alt="Store Logo Placeholder"`, `alt="Box Placeholder"`, `alt="Logo Placeholder"` with `alt=""` (decorative fallback images should have empty alt).

- [ ] **Step 3: Commit**

```bash
git add src/components/home/hero-carousel.tsx src/components/stores/store-view.tsx
git commit -m "fix(a11y): improve alt text for carousel and placeholder images"
```

### Task 7.3: Final build verification

- [ ] **Step 1: TypeScript check**

Run: `npx tsc --noEmit`
Expected: No errors

- [ ] **Step 2: Production build**

Run: `npm run build`
Expected: Successful build, all routes compiled

- [ ] **Step 3: Final commit (if any remaining changes)**

```bash
git add -A
git commit -m "chore: final polish pass — audit fixes complete"
```

---

## Verification

After all waves, re-run `/impeccable:audit` to measure improvement. Target scores:

| Dimension | Before | Target |
|-----------|--------|--------|
| Accessibility | 1 | 3 |
| Performance | 2 | 3 |
| Theming | 1 | 3 |
| Responsive | 2 | 3 |
| Anti-Patterns | 2 | 3 |
| **Total** | **8/20** | **15/20** |
